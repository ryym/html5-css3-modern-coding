{
  "private": true,
  "name": "html5-css3-modern-coding",
  "version": "1.0.0",
  "description": "Learning modern coding of HTML5/CSS3!",
  "scripts": {
    "prestart": "npm install",
    "start": "browser-sync start --config bs-config.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "browser-sync": "^2.13.0"
  }
}
